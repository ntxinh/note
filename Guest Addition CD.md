```
sudo apt install -y dkms build-essential linux-headers-generic linux-headers-$(uname -r)

Mount the Guest Additions CD ROM
sudo mount /dev/cdrom /media/cdrom

ls -l /media/cdrom/

sudo /media/cdrom/VBoxLinuxAdditions.run

```
